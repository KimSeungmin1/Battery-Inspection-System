# Battery Inspection System - Environment Variables
# 배터리 검사 시스템 - 환경 변수 설정
# ================================================
# Copy this file to .env and configure as needed.
# 이 파일을 .env로 복사한 뒤 필요한 값을 설정하세요.
# .env is gitignored and will not be committed.
# .env 파일은 Git에 커밋되지 않습니다.
# ================================================

# Data path overrides (optional)
# 데이터 경로 오버라이드 (선택사항)
# Default: config.yaml uses relative path (data/data_resized)
# 기본값: config.yaml의 상대경로(data/data_resized) 사용
# Set these only when your data is stored elsewhere.
# 데이터가 다른 경로에 있을 때만 설정하세요.

# Full path to the data_resized directory
# 전체 데이터 폴더 경로 (data_resized 폴더까지)
# Example: DATA_DIR=C:/data/battery-inspection/data_resized
# DATA_DIR=

# Base directory for relative paths in config.yaml
# config.yaml 상대경로의 기준 디렉터리
# Example: DATA_BASE_DIR=C:/data/battery-inspection
# DATA_BASE_DIR=
